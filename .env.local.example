# Supabase (mesmas chaves do site atual do Somma). Copie para .env.local.
# Sem estas variáveis, o formulário ainda funciona (sucesso visual), mas não grava o lead.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Google Maps (Embed API)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Asaas (checkout)
ASAAS_API_KEY=
